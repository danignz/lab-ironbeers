<div class='beer'>
  {{#if image_url}}
    <img src='{{image_url}}' alt='{{name}}' />
  {{else}}
    <img src='images/beer.png' alt='Not image avariable' />
  {{/if}}
  <div>
    <a href='/beers/{{id}}'><h1>{{name}}</h1></a>
    <p>{{tagline}}</p>
    <p>{{description}}</p>
  </div>
</div>